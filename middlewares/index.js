const stateCode = require('./../stateCode.json');

function constructReturnData(req, res, next) {
  // res.json({
  //   code,
  //   msg: stateCode[code],
  //   data
  // })
  // next()
}

module.exports = constructReturnData